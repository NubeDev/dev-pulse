//! `dp-domain` — entity types and the `Store` trait for dev-pulse.
//!
//! Stage 1 scaffold: this crate is intentionally empty. Entities and
//! the `Store` trait land in stage 4 (Phase 1, TODO §Phase 1).
//!
//! Hard rule (TODO §0.6): zero `starter_*` imports in this crate. The
//! `scripts/check-boundaries.sh` script enforces this in CI from
//! stage 2 onward.
